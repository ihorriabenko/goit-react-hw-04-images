import React from 'react';
import ReactDOM from 'react-dom/client';
import FindImages from './modules/FindImages';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  // <React.StrictMode>
    <FindImages />
  // </React.StrictMode>
);
